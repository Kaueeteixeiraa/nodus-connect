FROM node:24-slim

WORKDIR /app
RUN corepack enable

COPY package.json pnpm-lock.yaml pnpm-workspace.yaml tsconfig.json ./
COPY packages ./packages
COPY services ./services

RUN pnpm install --frozen-lockfile

ENV HOST=0.0.0.0
ENV PORT=8787
EXPOSE 8787

CMD ["pnpm", "start:relay"]
